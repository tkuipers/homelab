apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # metallb, cert-manager, ingress-nginx, csi-driver-smb now managed by ArgoCD
  # monitoring (loki, prometheus) removed - Datadog covers this
  - ./metrics-server
  - ./aws-ip-updater
  - ./external-dns
  - ./local-path-provisioner
