apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: flux-system
spec:
  interval: 5m
  dependsOn:
    - name: cert-manager
      namespace: cert-manager
  chart:
    spec:
      chart: ingress-nginx
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
      version: ">=4.0.0"
  install:
    createNamespace: true
  targetNamespace: ingress-nginx
  values:
    # Configure TCP services
    tcp:
      445: "timemachine/timemachine:445"
    controller:
      service:
        type: LoadBalancer
      ingressClassResource:
        name: nginx
        enabled: true
      admissionWebhooks:
        enabled: true
        patch:
          enabled: true
        certManager:
          enabled: false
        # Disable strict path validation to allow ACME challenges
        annotations:
          nginx.ingress.kubernetes.io/enable-annotation-validation: "false"
        # Set failure policy to Ignore for ACME challenges
        failurePolicy: Ignore
      # Enable TCP services
      config:
        strict-validate-path-type: "false"

      # TCP services are now configured via the tcp: key above
    rbac:
      create: true
      scope: false 