apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: ml
spec:
  selector:
    app: open-webui
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

