apiVersion: apps/v1
kind: Deployment
metadata:
  name: open-webui
  namespace: ml
spec:
  replicas: 1
  selector:
    matchLabels:
      app: open-webui
  template:
    metadata:
      labels:
        app: open-webui
    spec:
      containers:
      - name: open-webui
        image: ghcr.io/open-webui/open-webui:main
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: OPENAI_API_BASE_URLS
          value: "http://llama-server.ml.svc.cluster.local:8080/v1"
        - name: OPENAI_API_KEYS
          valueFrom:
            secretKeyRef:
              name: llama-api-key
              key: api-key
        - name: WEBUI_AUTH
          value: "false"
        - name: ENABLE_SIGNUP
          value: "false"
        # Web search configuration
        - name: ENABLE_RAG_WEB_SEARCH
          value: "true"
        - name: RAG_WEB_SEARCH_ENGINE
          value: "duckduckgo"
        - name: ENABLE_SEARCH_QUERY
          value: "true"
        # Agent web search via Brave Search API
        - name: ENABLE_WEB_SEARCH
          value: "true"
        - name: WEB_SEARCH_ENGINE
          value: "brave"
        - name: BRAVE_SEARCH_API_KEY
          valueFrom:
            secretKeyRef:
              name: mcp-api-keys
              key: brave-api-key
        - name: RAG_WEB_SEARCH_RESULT_COUNT
          value: "5"
        - name: RAG_WEB_SEARCH_CONCURRENT_REQUESTS
          value: "10"
        # Tool/function calling support
        - name: ENABLE_OPENAI_API
          value: "true"
        # RAG enhancements
        - name: ENABLE_RAG
          value: "true"
        - name: RAG_EMBEDDING_ENGINE
          value: "openai"
        - name: RAG_EMBEDDING_MODEL
          value: "nomic-embed-text"
        - name: ENABLE_IMAGE_GENERATION
          value: "false"
        # Enable community sharing
        - name: ENABLE_COMMUNITY_SHARING
          value: "true"
        volumeMounts:
        - name: data
          mountPath: /app/backend/data
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
      
      volumes:
      - name: data
        hostPath:
          path: /var/persist/open-webui
          type: DirectoryOrCreate

