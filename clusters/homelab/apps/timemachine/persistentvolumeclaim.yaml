apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: timemachine-storage
  namespace: timemachine
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Gi  # Adjust based on your backup needs 