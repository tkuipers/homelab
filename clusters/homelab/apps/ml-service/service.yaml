apiVersion: v1
kind: Service
metadata:
  name: llama-server
  namespace: ml-service
spec:
  selector:
    app: llama-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

