apiVersion: v1
kind: Service
metadata:
  name: matter-server
  namespace: homeassistant
  labels:
    app: matter-server
spec:
  selector:
    app: matter-server
  ports:
    - name: ws
      port: 5580
      targetPort: 5580
      protocol: TCP
  type: ClusterIP
