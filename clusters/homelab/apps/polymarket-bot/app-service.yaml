apiVersion: v1
kind: Service
metadata:
  name: bot
  namespace: polymarket-bot
  labels:
    app: polymarket-bot
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: polymarket-bot
