apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: polymarket-bot
  labels:
    app: polymarket-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: polymarket-postgres
