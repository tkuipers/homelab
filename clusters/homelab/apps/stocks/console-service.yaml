apiVersion: v1
kind: Service
metadata:
  name: console
  namespace: stocks
  labels:
    app: stocks-console
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: stocks-console
