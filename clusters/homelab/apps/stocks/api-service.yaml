apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: stocks
  labels:
    app: stocks-api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: stocks-api
