apiVersion: v1
kind: Service
metadata:
  name: llm-gateway
  namespace: stocks
  labels:
    app: stocks-llm-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: stocks-llm-gateway
