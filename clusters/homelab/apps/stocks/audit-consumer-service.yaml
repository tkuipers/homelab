apiVersion: v1
kind: Service
metadata:
  name: audit-consumer
  namespace: stocks
  labels:
    app: stocks-audit-consumer
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: stocks-audit-consumer
