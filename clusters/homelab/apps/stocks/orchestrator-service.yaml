apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: stocks
  labels:
    app: stocks-orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http
  selector:
    app: stocks-orchestrator
