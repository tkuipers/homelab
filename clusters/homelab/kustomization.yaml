apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Note: cert-manager and metallb are now managed by ArgoCD
  # Flux resources kept for remaining apps during migration
  - infrastructure
  - apps
  - flux-system
