apiVersion: v1
kind: Service
metadata:
  name: op-connect
  namespace: default
spec:
  selector:
    app: op-connect
  ports:
    - name: connect-api
      port: 8080
      targetPort: 8080
      protocol: TCP 